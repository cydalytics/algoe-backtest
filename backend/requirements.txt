# Algo E backend - offline install
#
# The offline machine already runs the extraction notebook, so pandas,
# numpy, scipy and pyodbc are normally present and only the web layer is
# missing. Install from a local wheel directory, never from the network:
#
#     pip install --no-index --find-links vendor -r requirements.txt
#
# Versions below are the ones this was developed and tested against. They
# are floors, not pins: if the offline machine already has something newer
# from the notebook environment, leave it alone.

# --- pipeline ---------------------------------------------------------
pandas>=2.0          # tested on 3.0.3
numpy>=1.24          # tested on 2.4.6
scipy>=1.10          # tested on 1.18.0 - SLSQP, the optimizer

# --- API --------------------------------------------------------------
fastapi>=0.110       # tested on 0.135.3
uvicorn>=0.27        # tested on 0.44.0

# --- data ------------------------------------------------------------
pyarrow>=14          # tested on 17.0.0 - reading the raw parquet, and the
                     # per-day panel cache the backtest page builds
pyodbc>=5.0          # live SQL Server; not needed for sim or capture
